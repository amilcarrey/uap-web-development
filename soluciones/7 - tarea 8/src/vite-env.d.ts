/// <reference types="vite/client" />
// Esta línea es una directiva de TypeScript que indica que se deben incluir
// las definiciones de tipo proporcionadas por Vite para el entorno del cliente (navegador).
// Esto permite que TypeScript reconozca variables y tipos especiales que Vite agrega,
// como `import.meta.env`, entre otros, evitando errores de tipo al compilar.
