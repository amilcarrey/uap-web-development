export interface updateBoardDTO{
    name?: string; // Este campo es opcional, ya que no siempre se necesita actualizar el nombre del board
    active?: boolean; // Este campo es opcional, ya que no siempre se necesita actualizar el estado activo del board
}
