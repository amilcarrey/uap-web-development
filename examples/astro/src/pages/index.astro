---
// Importar layout y controladores
import Layout from "../layouts/Layout.astro";
import { getTasks } from "../controllers/getTasks.js";
import { addTask } from "../controllers/addTask.js";
import { updateTask } from "../controllers/updateTask.js";
import { deleteTask } from "../controllers/deleteTask.js";
import { clearCompleted } from "../controllers/clearCompleted.js";
import { state } from "../state.js";

// Manejo de solicitudes POST
if (Astro.request.method === "POST") {
  const formData = await Astro.request.formData();

  const done = formData.get("done");
  const undone = formData.get("undone");
  const all = formData.get("all");

  if (done) {
    state.filter = "done";
  } else if (undone) {
    state.filter = "undone";
  } else if (all) {
    state.filter = "all";
  }

  const taskText = formData.get("taskInput");
  const completeTask = formData.get("completeTask");
  const deleteTaskId = formData.get("deleteTask");
  const clear = formData.get("clearCompleted");

  if (taskText) {
    addTask(taskText);
  } else if (completeTask) {
    updateTask(completeTask);
  } else if (deleteTaskId) {
    deleteTask(deleteTaskId);
  } else if (clear) {
    clearCompleted();
  }

  return Astro.redirect("/");
}

// Obtener tareas según el filtro actual
const tasks = getTasks(state.filter);
---
<Layout>
  <head>
    <title>Gestor de tareas</title>
  </head>
  <body>
    <main class="principal">
      <header class="encabezado">
        <div class="top">
          <h1 class="titulo">Gestor de tareas</h1>
        </div>
      </header>

      <nav class="navEncabezado">
        <div class="personal"><h3>Personal</h3></div>
        <div class="profesional"><h3>Profesional</h3></div>
      </nav>

      <!-- Formulario para agregar tarea -->
      <div class="agregar">
        <form method="post">
          <input type="text" name="taskInput" placeholder="Qué necesitas hacer?" required>
          <button type="submit" class="agg" name="addTask" value="true">AGREGAR</button>
        </form>
      </div>

      <!-- Lista de tareas -->
      <div class="lista">
        <ul id="checklist">
          {tasks.map(task => (
            <li>
              <div class="tarea">
                <form method="POST">
                  <button type="submit" name="completeTask" value={task.id} class="icono">
                    {task.completed ? "✅" : "⬜️"}
                  </button>
                </form>
                <span class={task.completed ? "completado" : ""}>
                  {task.text}
                </span>
                <form method="POST">
                  <button type="submit" name="deleteTask" value={task.id} class="icono">🗑️</button>
                </form>
              </div>
            </li>
          ))}
        </ul>
      </div>

      <!-- Botón limpiar completadas -->
      <div class="clear">
        <form method="POST">
          <button type="submit" name="clearCompleted" value="true" class="limpiar">Limpiar</button>
        </form>
      </div>

      <!-- Filtros de tareas -->
      <div class="botones">
        <form method="POST">
          <button type="submit" name="all" value="true" class="todo">Todas</button>
          <button type="submit" name="done" value="true" class="completado">Completas</button>
          <button type="submit" name="undone" value="true" class="incompleto">Incompletas</button>
        </form>
      </div>
    </main>
  </body>
</Layout>

<style>
 * {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: "Courier New", Courier, monospace;
}

body {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background-color: #f5f5dc; /* Fondo beige */
}

.principal {
  width: 100%;
  max-width: 600px;
  background: #fff;
  padding: 20px;
  border-radius: 8px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

.encabezado {
  text-align: center;
  background-color: #e49d89dc;
  padding: 10px;
}


.navEncabezado{
    display: flex;
    text-align: center;
    align-items: center;
    justify-content: space-around;
    background-color: #e8af9695;
    padding: 20px 20px;
}

.navEncabezado .personal, .navEncabezado .profesional {
    font-size: 18px;
    cursor: pointer;
}

.navEncabezado .personal:hover, .navEncabezado .profesional:hover {
    /* background-color: aqua; */
    border-bottom: 3px solid #e08123;

}

.titulo {
  font-size: 2.5em;
  color: black;
}

.agregar {
  display: flex;
  justify-content: space-between;
  padding: 10px;
  background: #efd8a4;
}

.agregar input {
  flex-grow: 1;
  padding: 10px 100px;
  margin-left: 35px;
  border: 1px solid #ccc;
  border-radius: 5px;
}

.agregar button {
  background: #a8c99e;
  color: white;
  border: none;
  padding: 8px 15px;
  border-radius: 5px;
  cursor: pointer;
}

.agregar button:hover {
  background: #88c874;
}


#checklist{
    list-style-type: none;
    padding: 0;
}



.lista {
  background-color: #efd8a4;
  max-width: 600px;
  margin: 20px auto;
  border-radius: 8px;
  padding: 20px;
  text-align: center;
}

.tarea {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 10px;
  background: #fff;
  border-radius: 5px;
  margin-bottom: 10px;
}

.icono {
  background: none;
  border: none;
  cursor: pointer;
  font-size: 1.2rem;
}

.completado {
  text-decoration: line-through;
  opacity: 0.5;
}

.clear {
  text-align: center;
}

.limpiar {
  background: #e49d89dc;
  border: none;
  padding: 8px 15px;
  border-radius: 5px;
  cursor: pointer;
  font-size: 1rem;
}

.limpiar:hover {
  background: #cc7a5a;
}

.botones {
  display: flex;
  justify-content: space-around;
  margin-top: 10px;
}

.botones button {
  background: #e8af9695;
  border: none;
  padding: 8px 15px;
  border-radius: 5px;
  cursor: pointer;
  font-size: 1rem;
}

.botones button:hover {
  background: #d89e7c;
}
</style>