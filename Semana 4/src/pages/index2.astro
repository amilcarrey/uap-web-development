---
import Layout from "../layouts/Layout.astro";
import { state, generateId } from "../state";

// import { addTask } from "../components/Task.ts";

let taskList: string[] = [];

if (Astro.request.method === "POST") {
	try {
    	const formdata = await Astro.request.formData();
    	const taskText = formdata.get("task")?.toString().trim();
		const taskId = formdata.get("task-id")?.toString();
		const taskButton = formdata.get("task-btn")?.toString();
		const deleteButton = formdata.get("delete-btn")?.toString();
		const clearCompleted = formdata.get("clear-completed")?.toString();
    
		if (taskText) {
			state.tasks.push({id: generateId(), text: taskText, done: false});
		}
		// else {  
		// 	return alert("Please enter a task.");
  		// }

		if (taskButton) {
			const task = state.tasks.find(t => t.id === taskId);
			if (task) {
				task.done = !task.done;
			}
		}

		if (deleteButton) {
			const taskIndex = state.tasks.findIndex(t => t.id === taskId);
			if (taskIndex > -1) {
				taskList.splice(taskIndex, 1);
			}
		}

		if (clearCompleted) {
			taskList = taskList.filter(task => task !== "✅");
		}
	}
	catch (error) {
		console.error("Error processing form data:", error);
	}
}
---
<Layout>
    <body>
        <!-- Encabezado de la página que contiene el título de la aplicación -->
        <header>
            <!-- Título principal con un diseño simple donde "TO" y "DO" están separados para dar énfasis -->
            <h1><span>TO</span>DO</h1>
        </header>

        <!-- Barra de navegación con botones para diferentes categorías o acciones -->
        <nav>
            <!-- Botón para la categoría "Personal" -->
            <button class="nav-btn">Personal</button>
            <!-- Botón para la categoría "Professional" (sin clase) -->
            <button>Professional</button>
            <!-- Botón para añadir nuevas tareas -->
            <button class="add-btn">+</button>
        </nav>

        <!-- Filtros de tareas, para filtrar entre todas, incompletas y completadas -->
        <div class="filters">
            <!-- Botón para mostrar todas las tareas -->
            <button class="filter-btn" data-filter="all">All</button>
            <!-- Botón para mostrar solo las tareas incompletas -->
            <button class="filter-btn" data-filter="incomplete">Incomplete</button>
            <!-- Botón para mostrar solo las tareas completadas -->
            <button class="filter-btn" data-filter="completed">Completed</button>
        </div>

        <!-- Contenido principal de la página -->
        <main>
            <!-- Sección para añadir nuevas tareas -->
            <div class="task-input">
                <form method="POST">
                    <!-- Campo de entrada de texto para escribir nuevas tareas -->
                    <input type="text" name="task" placeholder="What do you need to do?">
                    <!-- Botón para añadir la tarea -->
                    <button type="submit" class="add-task">ADD</button>
                </form>
            </div>

            <!-- Lista de tareas -->
            <ul class="task-list">
                <!-- Cada elemento de la lista representa una tarea -->
                <li>
					<form method="POST">
						<label>
							<!-- Botón para marcar la tarea como completada (inicialmente marcada como completada con "✅") -->
							<button class="task-btn" name="task-btn">✅</button>
							<!-- Descripción de la tarea -->
							<span>Personal Work No. 1</span>
						</label>
						<!-- Botón para eliminar la tarea -->
						<button class="delete-btn" name="delete-btn">🗑️</button>
					</form>
                </li>
                <li>
                    <form method="POST">
						<label>
							<!-- Botón para marcar la tarea como completada (inicialmente marcada como completada con "✅") -->
							<button class="task-btn" name="task-btn">✅</button>
							<!-- Descripción de la tarea -->
							<span>Personal Work No. 2</span>
						</label>
						<!-- Botón para eliminar la tarea -->
						<button class="delete-btn" name="delete-btn">🗑️</button>
					</form>
                </li>
                <li>
                    <form method="POST">
						<label>
							<!-- Botón para marcar la tarea como completada (inicialmente marcada como completada con "✅") -->
							<button class="task-btn" name="task-btn">✅</button>
							<!-- Descripción de la tarea -->
							<span>Personal Work No. 3</span>
						</label>
						<!-- Botón para eliminar la tarea -->
						<button class="delete-btn" name="delete-btn">🗑️</button>
					</form>
                </li>
                <li>
                    <form method="POST">
						<label>
							<!-- Botón para marcar la tarea como completada (inicialmente marcada como completada con "✅") -->
							<button class="task-btn" name="task-btn">✅</button>
							<!-- Descripción de la tarea -->
							<span>Personal Work No. 4</span>
						</label>
						<!-- Botón para eliminar la tarea -->
						<button class="delete-btn" name="delete-btn">🗑️</button>
					</form>
                </li>
                <li>
                    <form method="POST">
						<label>
							<!-- Botón para marcar la tarea como completada (inicialmente marcada como completada con "✅") -->
							<button class="task-btn" name="task-btn">✅</button>
							<!-- Descripción de la tarea -->
							<span>Personal Work No. 5</span>
						</label>
						<!-- Botón para eliminar la tarea -->
						<button class="delete-btn" name="delete-btn">🗑️</button>
					</form>
                </li>
				{state.tasks.map(task => (
					<li>
                    	<form method="POST">
							<input type="hidden" name="task-id" value={task.id} />
							<label>
								<button type="submit" class="task-btn" name="task-btn">{task.done ? "✅" : "⬜"}</button>
								<span>{ task.text }</span>
							</label>
							<button class="delete-btn" name="delete-btn">🗑️</button>
						</form>
                	</li>
				  ))}
            </ul>

            <!-- Botón para eliminar todas las tareas completadas -->
            <button class="clear-completed">Clear Completed</button>
        </main>
    </body>
</Layout>


<style is:global>
	/* Estilos generales */
	body {
		font-family: Arial, sans-serif; /* Se usa Arial o una fuente sans-serif como fallback */
		background-color: #f4ede6; /* Color de fondo del cuerpo */
		color: #555; /* Color del texto general */
		text-align: center; /* Alineación del texto al centro */
		margin: 0; /* Se elimina el margen predeterminado */
	}

	/* Título */
	header h1 {
		font-size: 32px; /* Tamaño del texto del título */
		color: #333; /* Color del texto */
		margin: 20px 0; /* Espaciado superior e inferior */
	}

	header h1 span {
		color: #e08e36; /* Se cambia el color de una parte del texto */
	}

	/* Barra de navegación */
	nav {
		display: flex; /* Se usa flexbox para distribuir los elementos */
		justify-content: space-between; /* Los elementos se distribuyen uniformemente en la barra */
		background-color: #fff; /* Fondo blanco */
		padding: 10px; /* Espaciado interno */
		border-bottom: 2px solid #ddd; /* Borde inferior para separar visualmente la barra */
	}

	nav button {
		border: none; /* Se elimina el borde */
		background: none; /* Se deja el fondo transparente */
		font-size: 18px; /* Tamaño del texto */
		padding: 10px 20px; /* Espaciado interno */
		cursor: pointer; /* Se cambia el cursor a puntero al pasar sobre el botón */
	}

	nav .nav-btn {
		font-weight: bold; /* Texto en negrita */
		border-bottom: 3px solid #e08e36; /* Línea inferior para destacar el botón activo */
	}

	.add-btn {
		background-color: #b07c7c; /* Color de fondo */
		color: white; /* Texto en color blanco */
		font-size: 20px; /* Tamaño del texto */
		padding: 5px 15px; /* Espaciado interno */
		border-radius: 5px; /* Bordes redondeados */
	}

	/* Input de tareas */
	.task-input {
		margin: 20px auto; /* Espaciado superior e inferior, centrado horizontalmente */
		display: flex; /* Se usa flexbox para alinear elementos */
		justify-content: center; /* Centramos el contenido */
		align-items: center; /* Alineación vertical */
	}

	.task-input input {
		width: 60%; /* Ocupa el 60% del ancho disponible */
		padding: 10px; /* Espaciado interno */
		border: none; /* Se elimina el borde */
		border-radius: 20px; /* Bordes redondeados */
		background-color: #eadecf; /* Color de fondo */
	}


	.add-task {
		background-color: #65b8d8; /* Color de fondo */
		color: white; /* Color del texto */
		border: none; /* Se elimina el borde */
		padding: 10px 20px; /* Espaciado interno */
		margin-left: 10px; /* Margen a la izquierda para separación */
		border-radius: 20px; /* Bordes redondeados */
		cursor: pointer; /* Se cambia el cursor a puntero */
	}

	.add-task:hover {
		background-color: #4a9cbd; /* Color más oscuro al pasar el cursor */
	}

	/* Lista de tareas */
	.task-list {
		list-style: none; /* Se eliminan los estilos de lista predeterminados */
		padding: 0; /* Se elimina el padding */
		margin: 0 auto; /* Centrado horizontalmente */
		width: 60%; /* Ancho de la lista */
		background-color: #e4dfd9; /* Color de fondo */
		border-radius: 10px; /* Bordes redondeados */
		padding: 20px; /* Espaciado interno */
	}

	.task-list li {
		display: flex; /* Se usa flexbox */
		align-items: center; /* Alineación vertical */
		justify-content: space-between; /* Espaciado uniforme */
		padding: 10px; /* Espaciado interno */
		border-bottom: 1px solid #ccc; /* Línea divisoria entre tareas */
	}

	.task-btn {
		background: none; /* Sin fondo */
		border: none; /* Sin borde */
		font-size: 18px; /* Tamaño del texto */
		cursor: pointer; /* Se cambia el cursor a puntero */
	}

	.delete-btn {
		background: none; /* Sin fondo */
		border: none; /* Sin borde */
		font-size: 18px; /* Tamaño del texto */
		cursor: pointer; /* Se cambia el cursor a puntero */
	}

	/* Asegurar que la barra de navegación y filtros están separados */
	.filters {
		display: flex; /* Se usa flexbox */
		justify-content: center; /* Centrado horizontal */
		gap: 10px; /* Espaciado entre los botones */
		margin-top: 10px; /* Espaciado superior */
		padding: 10px; /* Espaciado interno */
	}

	.filter-btn {
		padding: 10px 15px; /* Espaciado interno */
		border: none; /* Sin borde */
		border-radius: 5px; /* Bordes redondeados */
		cursor: pointer; /* Se cambia el cursor a puntero */
		background-color: #b07c7c; /* Color de fondo */
		color: white;
		font-size: 16px;
	}

	.filter-btn:hover {
		background-color: #916666; /* Color más oscuro al pasar el cursor */
	}


	.clear-completed {
		display: block; /* Se muestra como un bloque */
		margin: 20px auto; /* Centrado horizontalmente */
		padding: 10px 15px; /* Espaciado interno */
		background-color: #d9534f; /* Color de fondo */
		color: white; /* Color del texto */
		border: none; /* Sin borde */
		border-radius: 5px; /* Bordes redondeados */
		cursor: pointer; /* Se cambia el cursor a puntero */
	}

	.clear-completed:hover {
		background-color: #c9302c; /* Color más oscuro al pasar el cursor */
	}
</style>